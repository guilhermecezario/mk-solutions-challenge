import styled from 'styled-components';
import { ButtonLink as ButtonLinkGlobal } from '../../../stylesheets/global';

export const ButtonLink = styled(ButtonLinkGlobal)`
  margin: 30px 0px;
`;

export const Label = styled.label`
  display: block;

  font-size: .9rem;
  font-weight: bold;

  margin-bottom: 15px;

  color: #205366;
`;
